1.  登录：
    输入用户名、手机号、邮箱后直接判断是否存在
    用用户名注册后，可以设置邮箱和手机（所以不能仅根据用户名判断是否可以注册，可以用户名是a，但是手机号重了）
    用手机号注册后，可以设置邮箱
    用邮箱注册后，可以设置手机号
2.  判断在Service中的每个参数是否为空

3. 逐个mapper判断哪些可以做动态sql

4. 添加事物控制


6.  库存和销量不应该下单就更改，应该支付后更改，库存下单后更改，销量支付后更改，取消支付

7.  没有为订单详情单独做一个查询，而是直接遍历了用户的所有订单，虽然成功，但是效率有问题

8.  订单里有运费，未支付，打开我的订单，显示的是没运费

9.  地址修改bug
